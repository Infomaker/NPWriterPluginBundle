const pluginId = 'se.infomaker.marker';
const pluginName = 'marker';

const commands = {
    NODE: 'web only text',
};

const types = {
    NODE: 'alma-web-only-text',
};

const newsmlTags = {
    NODE: 'x-im-marked-text',
};

const keyboardShortcut = {
    MAIN: 'ctrl+p',
    MAC: 'cmd+p',
};

export {
    pluginId,
    pluginName,
    commands,
    types,
    newsmlTags,
    keyboardShortcut,
};
